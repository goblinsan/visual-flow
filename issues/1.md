### Problem

**1. 401 Error Instead of Login Prompt**
Currently, when editing a palette and saving without being logged in, a "401 Failed to Save" error occurs instead of prompting the user to log in.

Expected behavior:
- If the user is not logged in, the application should pause the save action.
- Prompt the user to log in.
- After logging in, the application should resume the save action seamlessly.

**2. Details Button Styling and Placement Issues**
- The "Details" button in the hamburger menu has inconsistent styling with other menu items.
- The "Details" button is also expected at the bottom of the design page, styled in the same manner as other buttons.

### Steps to Fix

1. Update the save palette logic to handle `401 Unauthorized` by detecting the logged-out state and implementing the login flow.
2. Apply consistent styles to the "Details" button in the hamburger menu.
3. Add the "Details" button to the bottom of the design page with styling matching other buttons.

### Additional Context
- The pull request [#172](https://github.com/goblinsan/visual-flow/pull/172) partially addressed these features, but some issues remain unresolved and require further enhancement.
- Palette saving currently uses localStorage for persistence.