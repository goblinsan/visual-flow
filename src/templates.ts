/**
 * Canvas design templates used by the welcome modal, template browser, and file menu.
 *
 * Extracted from CanvasApp.tsx to reduce the god-component's line count.
 */
export { TEMPLATES } from './templates.data';
export type { CanvasTemplate } from './templates.data';
